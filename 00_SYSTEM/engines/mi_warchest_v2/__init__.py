"""
MI Warchest v2 — Michigan legal authority arsenal (DEPRECATED).

Superseded by the search engine (tantivy + FTS5 hybrid) and
authority_chains_v2 table in litigation_context.db.
Retained as reference for topic taxonomy.
DO NOT USE — use search engine + litigation_legal_search instead.

Key resources:
    tools/       — Authority lookup and search utilities (reference only)
    schemas/     — Data schemas for authority records
    docs/        — Reference documentation
    topics.yaml  — Topic taxonomy for organized research
"""
__deprecated__ = True
__superseded_by__ = "search engine + authority_chains_v2"
