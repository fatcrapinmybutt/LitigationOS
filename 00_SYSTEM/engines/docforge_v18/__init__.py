"""
DocForge v18 — Document generation engine (DEPRECATED).

Superseded by the Typst engine and filing_engine pipeline.
Retained as reference for template patterns and manifest structure.
DO NOT USE for new filings — use filing_engine + typst instead.

Sub-packages:
    core/       — Document assembly logic (reference only)
    templates/  — Filing format templates (reference only)
    runtime/    — Execution environment and manifest handling
    output/     — Generated document output directory
"""
__deprecated__ = True
__superseded_by__ = "filing_engine + typst"
