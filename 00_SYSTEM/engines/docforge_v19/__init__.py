"""
DocForge v19 — Next-generation document forge engine (DEPRECATED).

Superseded by the Typst engine and filing_engine pipeline.
Retained as reference for schema patterns and template structure.
DO NOT USE for new filings — use filing_engine + typst instead.

Sub-packages:
    core/           — Document assembly and rendering logic (reference only)
    config/         — Engine configuration and feature flags
    templates/      — Filing and brief templates (reference only)
    schemas/        — JSON schemas for input validation
    runtime/        — Execution orchestration
    sample_inputs/  — Reference input examples
"""
__deprecated__ = True
__superseded_by__ = "filing_engine + typst"
