# AI optional module

OrganizerStudio operates safely without AI.

If you install optional AI dependencies, it can provide:
- semantic clustering for folder/category suggestions
- filename suggestion scoring (conservative; never auto-applied without plan preview)

Install optional dependencies:
- pip install -r requirements_ai.txt
