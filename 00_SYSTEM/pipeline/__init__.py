"""
LitigationOS Pipeline Package
===============================
16-phase OMEGA pipeline: inventory → dedup → classify →
extract → brain feed → gap analysis → graph → synthesis →
litigation refresh → MCP ingest → judicial → legal →
rule audit → refinement → finalization → validation.

Phases run via run_omega_pipeline.py or individually.
"""

__version__ = "1.0.0"
