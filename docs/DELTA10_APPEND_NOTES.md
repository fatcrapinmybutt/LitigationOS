# DELTA10 Main UI Integration Append

## What this append cycle adds
- Mounts **Desktop Corpus Intelligence** into the main Command Center shell.
- Adds **Canonical Lineage Graph** panel (SVG summary view).
- Adds **Timeline**, **Orders**, and **Vehicle Readiness** live data rails.
- Adds **Ops provenance overlay** (inventory/lineage/replay counts).
- Upgrades runtime launch to emit deterministic cycle IDs and replay events.

## Continuation targets
1. Order supersession graph from real order text and service proofs.
2. Evidence coverage scoring sourced from ExhibitMatrix / EvidenceAtoms.
3. Interactive lineage graph (filters, hover details, provenance jump links).
