{
  "id": "mem-020",
  "projectId": "litigationos",
  "key": "schema-gotchas",
  "type": "bug",
  "content": "DB schema gotchas: authority_chains.chain_complete (NOT is_complete). deadlines.due_date_iso (NOT deadline_date). claims.claim_id (NOT id). filing_readiness.vehicle_name (NOT vehicle). Product App schema.sql and MCP db.py define DIFFERENT schemas for shared tables. Always run PRAGMA table_info(table_name) before querying.",
  "tags": ["schema", "database", "gotchas", "bug"],
  "relationships": { "dependsOn": ["mem-004"], "implements": [] },
  "metadata": { "accessCount": 0, "createdBy": "copilot-session" },
  "createdAt": 1741478400000,
  "updatedAt": 1741478400000
}
