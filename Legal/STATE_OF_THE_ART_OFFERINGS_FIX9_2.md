# State-of-the-art (realistic) upgrades to wire in next

This pack adds a GUI (Tkinter) and keeps PathLengthGuard + marker-based root discovery.
The following are realistic, high-tech, and free/open-source aligned.

1) Plugin stage runner (plugins/ registry with typed contracts + validators)
2) SavePoint timeline UI (select checkpoint and resume stage)
3) Neo4j lifecycle manager (start/stop, apply schema, import runner)
4) Bloom perspective pack exporter/importer (perspectives, style presets, checklist)
5) Evidence bag packager (BagIt + optional RO-Crate metadata crate)
6) Observability (structured JSONL logs + progress + metrics)
7) Optional local semantic layer (auditable retrieval artifacts: query/hits/scores)
8) Safety rails (confirmations + audit log for destructive ops)
9) Optional scheduled runs (Task Scheduler generator, not auto-enabled)
10) One-file portable GUI.exe (PyInstaller build, console disabled)
