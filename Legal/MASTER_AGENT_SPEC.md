# MASTER_AGENT_SPEC — LitigationOS Agent (v6)

RebuttalAtoms -> RebuttalCompiler -> VehicleCandidates -> (OperatingOrderPin) -> DeadlineEval -> PacketBuild -> GraphUpsert -> Bloom

This pack is a SPEC + reference tools, not filings.

Core invariants:
- TruthLock: no invented facts; no invented authority pinpoints
- Deadline truth: compute only from pinned anchors + resolved authority registry values
- Packets are NOT filings: they are drafting outlines + exhibit maps + checklists
