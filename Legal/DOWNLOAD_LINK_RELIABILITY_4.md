# DOWNLOAD LINK RELIABILITY

This bundle is created under /mnt/data with a safe ASCII name.
Integrity gates used:
- ZIP integrity verified via zipfile.testzip()==None.
- SHA-256 computed for the ZIP.
- Bundle contains manifest.json and manifest.csv with per-file SHA-256.
